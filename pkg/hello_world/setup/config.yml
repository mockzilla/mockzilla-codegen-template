# yaml-language-server: $schema=https://raw.githubusercontent.com/mockzilla/connexions/refs/heads/master/resources/json-schema-service.json
name: hello-world
latency: 0ms
cache:
  requests: true
#upstream:
#  url: "https://hello-world.example"
#  timeout: 10s
#  circuit-breaker:
#    timeout: 60s        # Time in open state before half-open
#    max-requests: 1     # Max requests allowed in half-open state
#    interval: 0s        # Interval to clear counts in closed state (0 = never)
#    min-requests: 3     # Min requests before evaluating failure ratio
#    failure-ratio: 0.6  # Failure ratio threshold to trip (0.0-1.0)
#    trip-on-status:
#      - range: "500-599"
